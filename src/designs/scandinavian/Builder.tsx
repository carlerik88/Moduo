import { ShelfBuilder } from '../../components/builder';
import { PageWrapper } from '../../components/layout';

export default function ScandinavianBuilder() {
  return (
    <PageWrapper theme="scandinavian">
      <ShelfBuilder theme="scandinavian" />
    </PageWrapper>
  );
}
